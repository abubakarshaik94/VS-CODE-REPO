name: Run Manually

on:
  workflow_dispatch:

jobs:
  manual-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "This workflow was run manually!"

  manual-job-2:
    runs-on: ubuntu-latest
    needs: manual-job
    steps:
      - name: Print message
        run: echo "This workflow was run manually sometimes!"
